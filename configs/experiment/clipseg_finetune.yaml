# @package _global_

trainer:
  max_steps: 20000
  precision: 16 # for amp set to true
  max_epochs: 200

callbacks:
  model_checkpoint:
    dirpath: ${paths.output_dir}/checkpoints
    filename: "epoch_{epoch:03d}"
    monitor: "val/loss"
    mode: "min"
    save_last: True
    auto_insert_metric_name: False