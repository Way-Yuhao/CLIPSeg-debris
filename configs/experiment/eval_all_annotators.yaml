# @package _global_

defaults:
  - override /model: multi_annotation_reader
  - override /logger: csv
  - override /callbacks: null
  - override /data: debris_one_hot
  - _self_

test: true
predict: false

data:
    slit_dataset: all_to_test
    merge_via: majority_vote_734_negative # union

logger:
  csv:
    save_dir: "${local.prediction_output_dir}/inter-annotator"
    name: "csv_all/"
    prefix: ""
