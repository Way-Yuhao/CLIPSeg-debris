# @package _global_

defaults:
  - override /model: yolov8
  - _self_

test: true

data:
  merge_via: majority_vote_734_negative
  remove_ids: ['001477', '000391', '000393', '020818', '020976', '000134', '021722']

trainer:
  check_val_every_n_epoch: 10
  max_epochs: 1000

callbacks:
  model_checkpoint:
    monitor: "val/dice_debris"
    every_n_epochs: null
    save_top_k: 5