_target_: src.data.debris_datamodule.DebrisPredictDataModule
batch_size: 64
num_workers: 12
pin_memory: false

query_images_dir: null
mask: "text_and_crop_blur_highlight352"

# dataset parameters
dataset:
  _target_: src.data.components.debris_one_hot.DebrisPredictionDataset
  dataset_dir: ${data.query_images_dir}
  resize_to: [256, 256]
  text_prompts: ['no debris', 'debris at low density', 'debris at high density']
  densities: ['no', 'low', 'high']